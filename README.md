# Open or Close Sunshine & Tailscale
A pair of scripts that respectively open and close Sunshine and Tailscale on Linux systems.

> [!IMPORTANT]
> Run `sudo tailscale set --operator=$USER` to allow the scripts to connect and disconnect Tailscale.
